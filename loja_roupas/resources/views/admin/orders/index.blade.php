@extends('layouts.admin', ['heading' => 'Pedidos'])
@section('content')<div class="admin-toolbar"><p>Acompanhe os pedidos dos clientes.</p><a class="admin-button" href="{{ route('admin.orders.create') }}">+ Novo pedido</a></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Pedido</th><th>Cliente</th><th>Total</th><th>Status</th><th>Ações</th></tr></thead><tbody>@forelse ($orders as $order)<tr><td>#{{ $order->id }}</td><td>{{ $order->client->name }}</td><td>R$ {{ number_format((float) $order->total_amount, 2, ',', '.') }}</td><td><span class="status status-{{ $order->status }}">{{ ucfirst($order->status) }}</span></td><td class="actions"><a href="{{ route('admin.orders.show', $order) }}">Ver</a><a href="{{ route('admin.orders.edit', $order) }}">Editar</a><form method="POST" action="{{ route('admin.orders.destroy', $order) }}" onsubmit="return confirm('Excluir este pedido?')">@csrf @method('DELETE')<button>Excluir</button></form></td></tr>@empty<tr><td colspan="5" class="empty-table">Nenhum pedido cadastrado.</td></tr>@endforelse</tbody></table></div>{{ $orders->links() }}@endsection
