<div class="form-grid"><label>Cliente<select name="client_id" required><option value="">Selecione um cliente</option>@foreach ($clients as $client)<option value="{{ $client->id }}" @selected(old('client_id', $order->client_id ?? '') == $client->id)>{{ $client->name }}</option>@endforeach</select>@error('client_id')<small class="field-error">{{ $message }}</small>@enderror</label><label>Total<input name="total_amount" type="number" step="0.01" min="0" value="{{ old('total_amount', $order->total_amount ?? '') }}" required>@error('total_amount')<small class="field-error">{{ $message }}</small>@enderror</label><label>Status<select name="status" required>@foreach (['pending' => 'Pendente', 'paid' => 'Pago', 'shipped' => 'Enviado', 'cancelled' => 'Cancelado'] as $value => $label)<option value="{{ $value }}" @selected(old('status', $order->status ?? 'pending') === $value)>{{ $label }}</option>@endforeach</select></label></div><button class="admin-button" type="submit">{{ $buttonText }}</button><a class="cancel-link" href="{{ route('admin.orders.index') }}">Cancelar</a>
