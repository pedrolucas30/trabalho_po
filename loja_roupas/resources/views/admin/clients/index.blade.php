@extends('layouts.admin', ['heading' => 'Clientes'])
@section('content')<div class="admin-toolbar"><p>Cadastros e contatos dos clientes.</p><a class="admin-button" href="{{ route('admin.clients.create') }}">+ Novo cliente</a></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>Nome</th><th>E-mail</th><th>Telefone</th><th>Ações</th></tr></thead><tbody>@forelse ($clients as $client)<tr><td><strong>{{ $client->name }}</strong></td><td>{{ $client->email ?: 'Não informado' }}</td><td>{{ $client->phone ?: 'Não informado' }}</td><td class="actions"><a href="{{ route('admin.clients.show', $client) }}">Ver</a><a href="{{ route('admin.clients.edit', $client) }}">Editar</a><form method="POST" action="{{ route('admin.clients.destroy', $client) }}" onsubmit="return confirm('Excluir este cliente?')">@csrf @method('DELETE')<button>Excluir</button></form></td></tr>@empty<tr><td colspan="4" class="empty-table">Nenhum cliente cadastrado.</td></tr>@endforelse</tbody></table></div>{{ $clients->links() }}@endsection
